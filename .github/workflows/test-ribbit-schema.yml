name: Smoke Test

on:
  workflow_dispatch:
  schedule:
    - cron: "30 1 * * 6" # Run EoD friday (so ~1am UTC Saturday) to check if the schema has been patched this week (and email me)
    
permissions:
  contents: read
  actions: read
  checks: write

jobs:
  test:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: true    
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'
        dotnet-quality: 'preview'
    
    - name: Restore dependencies
      run: dotnet restore Minimaps.Tests
    
    - name: Build
      run: dotnet build Minimaps.Tests --no-restore
    
    - name: Test
      run: dotnet test Minimaps.Tests --filter "RibbitClientTests.*" --no-build --logger:"trx;LogFileName=test-results.trx"

    - name: Test Report
      uses: dorny/test-reporter@v1
      if: success() || failure()
      with:
        name: Ribbit Schema Test
        path: '**/test-results.trx'

        reporter: dotnet-trx